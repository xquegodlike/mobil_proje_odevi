import 'package:flutter/material.dart';

class TelevisionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Televizyon Sayfası"),
      ),
    );
  }
}
